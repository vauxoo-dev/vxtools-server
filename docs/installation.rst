============
Installation
============

At the command line::

    $ easy_install vxtools-server

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv vxtools-server
    $ pip install vxtools-server